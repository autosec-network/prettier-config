# `@autosec/prettier-config`

> Our [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ npm i -D autosec-network/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@autosec/prettier-config"
}
```
